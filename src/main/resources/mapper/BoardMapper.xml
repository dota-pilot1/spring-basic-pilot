<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyunseok.spring_basic_pilot.mapper.BoardMapper">

    <select id="findAll" resultType="com.hyunseok.spring_basic_pilot.domain.Board">
        SELECT b.id, b.title, b.content, b.author_id, b.hit, b.created_at, u.username as author_name
        FROM boards b
        LEFT JOIN users u ON b.author_id = u.id
        ORDER BY b.id DESC
    </select>

    <select id="findById" resultType="com.hyunseok.spring_basic_pilot.domain.Board">
        SELECT b.id, b.title, b.content, b.author_id, b.hit, b.created_at, u.username as author_name
        FROM boards b
        LEFT JOIN users u ON b.author_id = u.id
        WHERE b.id = #{id}
    </select>

    <select id="findBySearch" resultType="com.hyunseok.spring_basic_pilot.domain.Board">
        SELECT b.id, b.title, b.content, b.author_id, b.hit, b.created_at, u.username as author_name
        FROM boards b
        LEFT JOIN users u ON b.author_id = u.id
        <where>
            <if test="keyword != null and keyword != ''">
                AND (b.title LIKE CONCAT('%', #{keyword}, '%') OR b.content LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY b.id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countBySearch" resultType="long">
        SELECT COUNT(*)
        FROM boards b
        <where>
            <if test="keyword != null and keyword != ''">
                AND (b.title LIKE CONCAT('%', #{keyword}, '%') OR b.content LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
    </select>

    <insert id="insert" parameterType="com.hyunseok.spring_basic_pilot.domain.Board" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO boards (title, content, author_id)
        VALUES (#{title}, #{content}, #{authorId})
    </insert>

    <update id="update">
        UPDATE boards
        SET title = #{title},
            content = #{content}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM boards
        WHERE id = #{id}
    </delete>

    <update id="updateHit">
        UPDATE boards
        SET hit = hit + 1
        WHERE id = #{id}
    </update>

</mapper>
